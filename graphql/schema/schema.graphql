type UserProfile {
    id: String!
    email: String!
}

type FetchedUser {
    id: String!
    follow: [String]!
    permissions: [String]!
}

type User {
    profile: UserProfile!
    fetched: FetchedUser!
}

type Query {
    user: User
}